{
  "name": "movement-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "A small consent-based movement tracker with a live dashboard.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check app.js && node --check dashboard.js"
  },
  "engines": {
    "node": ">=20 <25"
  }
}
